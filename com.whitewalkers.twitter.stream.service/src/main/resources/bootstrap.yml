spring:
  application:
    name: whitewalkers-twitter-stream-service
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://whitewalkersconfigserver:8888 
      