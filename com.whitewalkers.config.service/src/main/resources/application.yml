server:
  port: 8888
spring:
  application:
    name: whitewalkers-config-service 
  profiles:
    active: native
  cloud:
     config:
       server:
           native:
              searchLocations:  classpath:configs/